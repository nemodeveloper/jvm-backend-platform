platform:
  core:
    logging:
      console-enabled: ${PLATFORM_CORE_LOGGING_CONSOLE_ENABLED:true}
      format: ${PLATFORM_CORE_LOGGING_FORMAT:plain}
      debug-mode-log-packages: ${PLATFORM_CORE_LOGGING_DEBUG_MODE_LOG_PACKAGES:ru.nemodev}